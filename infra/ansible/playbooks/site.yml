---
- name: Capstone lab baseline (site)
  hosts: all
  become: true
  tasks:
    - name: Ensure base packages
      ansible.builtin.package:
        name:
          - curl
          - jq
        state: present
